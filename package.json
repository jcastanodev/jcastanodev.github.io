{
  "name": "jcastanodev.github.io",
  "version": "1.0.0",
  "description": "Home page... (Working on it)",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"tailwindcss -i ./index.css -o ./tailwind.css --watch\" \"browser-sync start --server --files '**/*.html, **/*.css, **/*.js'\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^3.0.3",
    "jcastanodev.github.io": "file:",
    "tailwindcss": "^3.4.15"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}